/*
 * إعدادات الواجهة — عدّل هذا الملف لكل بيئة نشر.
 *
 * الواجهة تُنشر على استضافة مستقلة عن الخادم، لذلك يجب أن يكون العنوان مطلقاً.
 * الخادم يقابله في مجلد ../server.
 */
window.NEGEV_CONFIG = {
  // عنوان الخادم — غيّره إلى دومين الإنتاج عند النشر.
  apiBase: 'http://localhost:3000'
};
