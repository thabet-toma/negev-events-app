/*
 * إعدادات الواجهة.
 *
 * الافتراضي هو **نفس الأصل** (سلسلة فارغة): في كل نشر حقيقي يجلس الخادم
 * والواجهة خلف دومين واحد، وnginx يوجّه `/api` و`/uploads` و`/socket.io`
 * إلى الخادم. لذلك لا يحتاج هذا الملف تعديلاً عند النشر — وأي قيمة مثبَّتة
 * هنا كانت ستُكسر الإنتاج وتعود مع كل `git pull`.
 *
 * التطوير المحلي يعمل بنفس الإعداد: `web/serve.js` يمرّر تلك المسارات إلى
 * الخادم، فتطابق البيئتان.
 *
 * اضبط عنواناً مطلقاً هنا فقط إذا نشرت الواجهة على دومين منفصل عن الخادم —
 * وعندها أضف أصل الواجهة إلى `CORS_ORIGINS` في الخادم.
 */
window.NEGEV_CONFIG = {
  apiBase: '',

  /*
   * DSN متصفّح Sentry لتتبّع الأخطاء (تذكرة #44) — فارغ افتراضياً عمداً.
   * الخادم الذاتي المستضاف لتلقّي هذه التقارير لم يُنشر بعد ومكان استضافته
   * غير محسوم (خادم الإنتاج بمعالج واحد وswap ممتلئة أصلاً، فلا يحتمل خادماً
   * إضافياً الآن). قيمة فارغة هنا تعني عدم تتبّع إطلاقاً — لا سكربت CDN
   * يُحمَّل، لا اتصال، لا تكلفة (app.js يتحقق من هذه القيمة قبل حقن أي شيء).
   * اضبط DSN حقيقياً هنا فقط بعد أن يُنشر الخادم فعلياً.
   */
  errorDsn: '',

  /*
   * مُعرِّف نشرة حزمة الويب — يُرسَل مع كل تقرير خطأ لتمييز أي نشرة أُنتج
   * منها (لا خطوة بناء هنا، فلا رقم نشرة تلقائي؛ ارفعه يدوياً مع كل نشر حقيقي
   * لواجهة الويب).
   */
  releaseVersion: 'negev-web@1.0.0'
};
