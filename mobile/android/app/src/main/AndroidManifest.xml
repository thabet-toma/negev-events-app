<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- التطبيق واجهة فقط: كل بياناته من خادم بعيد، فبدون هذا الإذن لا يعمل
         إصدار release إطلاقاً (قالب فلاتر يضيفه في debug/profile فقط). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- لزرّ "موقعي الآن" في منتقي خريطة النشر فقط — لالتقاط لمرّة واحدة
         والتطبيق مفتوح. لا تتبّع خلفي ولا خدمة أمامية، فلا إذن ثالث. أندرويد
         12+ يتجاهل FINE بلا COARSE معه في نفس الطلب. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:label="مناسبات النقب"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links (issue #44): رابط مناسبة مشترَك يفتح على تفاصيلها
                 مباشرة بلا مربّع اختيار تطبيق — لا مخطط خاص (أي تطبيق يعلن
                 مخططاً)، بل تحقّق `https` حقيقي عبر autoVerify + الملف على
                 web/.well-known/assetlinks.json. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="munasbat.ktra-pro.tech"
                    android:pathPrefix="/e/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
